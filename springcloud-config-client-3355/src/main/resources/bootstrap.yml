# bootstrap.yml是系统级别的配置  而application.yml是用户级别的配置
spring:
  cloud:
    config:
      name: config-client #config-client.yml需要从github上面读取的资源名称，不需要后缀
      profile: test
      label: master #指的是分支
      uri: http://localhost:3344

